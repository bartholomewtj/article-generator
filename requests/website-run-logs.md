Log each website run/request as structured analytics so the process can be improved: one JSON line per /api/ideas, /api/draft, and /api/gallery (successes and failures). Always write the line to stderr. Optionally append it to a private gist. Never log the topic, theme, search terms, article HTML/markdown, API keys, or IP addresses.
Where: articlegen/web.py, articlegen/gallery.py, tests/test_offline.py, deploy/README.md, CLAUDE.md, docs/decisions.md, render.yaml
Done means: each of those endpoints emits one JSON line with time, endpoint, status, duration, model, public-vs-visitor key, and (for drafts) screened/cited/direct/related, full-text count and via, named-sources added, style issues, unverified/misattributed figures, working-draft brand, and error class; a private gist path exists behind an env var using the existing gist-scoped token; tests in test_offline.py pin that forbidden fields are absent; both test suites green.
Out of scope: writing visitor articles to drafts/ on the shared host; a public /api/runs endpoint; Google Analytics or other visitor page-view tracking; putting --long on the web UI; regenerating the public demo Reviews in drafts/; committing the untracked 2026-08-22 drafts or tools/compare_models.py; logging topics even hashed.
