__pycache__/
*.pyc
*.egg-info/
build/
dist/
.venv/
venv/
# local secrets — API keys and the contact address live here, never in the repo
.env
.env.*
!.env.example
# generated output (drafts/ is intentionally tracked — local review surface, not Pages)
# visitor gallery on a local `articlegen web` — hosted copy is a gist, not git
/gallery/
/*.html
!README*.html
/ideas/
!index.html

# sssf runtime
adws/adw_data/sessions/
adws/adw_data/sssf.db*

# auto-written by agent /init; CLAUDE.md is the working agreement
/AGENTS.md
/GEMINI.md

# Factory stays on this machine; not on GitHub
/adws/
/specs/
/requests/
/app_docs/
/justfile

# Public-repo extras moved off GitHub
/archive/
