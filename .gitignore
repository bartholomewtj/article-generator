__pycache__/
*.pyc
*.egg-info/
build/
dist/
.venv/
venv/
# local secrets — API keys and the contact address live here, never in the repo
.env
.env.*
!.env.example
# generated output (drafts/ is intentionally tracked — it's the review surface)
/*.html
!README*.html
/ideas/
!index.html
